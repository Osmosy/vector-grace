{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "grace status --path . --brief 2>/dev/null || echo 'GRACE: project artifacts present but grace-cli not installed. Install: bun add -g @osovv/grace-cli'"
          }
        ]
      }
    ]
  }
}
